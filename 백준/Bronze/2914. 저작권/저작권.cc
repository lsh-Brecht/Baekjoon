#include <iostream>
using namespace std;
void solve() {
	int a, b; cin >> a >> b; cout << a * b - (a - 1);
}
int main() {
	ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
	solve();
	return 0;
}